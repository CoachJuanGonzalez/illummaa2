  ---
  URGENT PROMPT FOR REPLIT: Fix Mobile Button Text Layout        

  PROBLEM: The mobile buttons still look unprofessional due      
  to conflicting CSS rules. The universal button optimization    
   is forcing text wrapping, but the hero-specific CSS is        
  fighting against it, creating awkward line breaks.

  REQUIRED FIXES:

  1. Replace Hero Button CSS (client/src/index.css)

  Find and REPLACE the .btn-primary-hero section (around         
  lines 769-805) with:

  /* MOBILE-FIRST Hero Primary Button */
  .btn-primary-hero {
    background: linear-gradient(135deg, #2C5530 0%, #1DB954      
  100%);
    transition: all 0.3s ease;
    padding: 12px 20px;
    min-height: 48px;
    width: 100%;
    font-weight: 600;
    font-size: 14px;
    border-radius: 12px;
    /* MOBILE: Allow smart text wrapping */
    white-space: normal !important;
    word-wrap: break-word !important;
    overflow-wrap: anywhere !important;
    text-align: center !important;
    line-height: 1.3 !important;
    /* Ensure text fits properly */
    display: flex !important;
    flex-direction: column !important;
    align-items: center !important;
    justify-content: center !important;
    gap: 4px !important;
  }

  /* MOBILE: Make button text more compact */
  @media (max-width: 480px) {
    .btn-primary-hero {
      padding: 10px 16px;
      font-size: 13px;
      min-height: 44px;
      line-height: 1.2;
    }

    /* Stack icon and text vertically on very small screens      
  */
    .btn-primary-hero .mr-3 {
      margin-right: 0 !important;
      margin-bottom: 2px !important;
    }
  }

  /* TABLET: Single line with proper spacing */
  @media (min-width: 481px) and (max-width: 639px) {
    .btn-primary-hero {
      padding: 14px 24px;
      font-size: 14px;
      /* Try to keep on one line */
      white-space: nowrap !important;
      overflow: hidden !important;
      text-overflow: ellipsis !important;
    }
  }

  /* DESKTOP: Normal button behavior */
  @media (min-width: 640px) {
    .btn-primary-hero {
      width: auto;
      padding: 16px 28px;
      min-height: 52px;
      font-size: 16px;
      white-space: nowrap;
      flex-direction: row;
      gap: 8px;
    }
  }

  2. Update Hero Secondary Button

  Find and REPLACE the .hero-secondary-btn-optimized section     
  (around lines 808-843) with:

  /* MOBILE-FIRST Hero Secondary Button */
  .hero-secondary-btn-optimized {
    background: rgba(255, 255, 255, 0.9);
    backdrop-filter: blur(8px);
    border: 1px solid rgba(44, 85, 48, 0.2);
    color: #1a365d;
    padding: 12px 20px;
    min-height: 48px;
    width: 100%;
    border-radius: 12px;
    font-weight: 600;
    font-size: 14px;
    /* MOBILE: Smart text handling */
    white-space: normal !important;
    text-align: center !important;
    line-height: 1.3 !important;
    display: flex !important;
    align-items: center !important;
    justify-content: center !important;
    gap: 6px !important;
  }

  @media (max-width: 480px) {
    .hero-secondary-btn-optimized {
      padding: 10px 16px;
      font-size: 13px;
      min-height: 44px;
    }
  }

  @media (min-width: 481px) and (max-width: 639px) {
    .hero-secondary-btn-optimized {
      padding: 14px 24px;
      font-size: 14px;
      white-space: nowrap;
    }
  }

  @media (min-width: 640px) {
    .hero-secondary-btn-optimized {
      width: auto;
      padding: 16px 28px;
      min-height: 52px;
      font-size: 16px;
      white-space: nowrap;
    }
  }

  3. Alternative Solution: Shorter Button Text for Mobile        

  UPDATE the hero-section.tsx button text (lines 55-56) to be    
   mobile-friendly:

  <span className="hidden sm:inline">Take Partnership &
  Learning Assessment</span>
  <span className="sm:hidden">Take Assessment</span>

  This will show:
  - Mobile: "Take Assessment" (shorter, fits better)
  - Desktop: "Take Partnership & Learning Assessment" (full      
  text)

  4. Test Requirements:

  - Verify buttons look professional on iPhone SE (320px)        
  - Ensure text doesn't wrap awkwardly on mobile
  - Confirm full functionality on tablet and desktop
  - Check that icons remain properly positioned

  PRIORITY: This is critical for mobile user experience. The     
  current awkward text wrapping damages the professional
  appearance of your enterprise website.

  ---

● ✅ COMPREHENSIVE MOBILE BUTTON FIX PROMPT READY

  The issue is conflicting CSS rules between the universal       
  button optimization and hero-specific styles. The prompt       
  above provides three solutions:

  🎯 Recommended Approach:

  Option 3 - Use shorter text on mobile: "Take Assessment"       
  instead of the full text. This is the cleanest solution
  that maintains professional appearance while ensuring
  perfect fit on all mobile devices.

  🔧 Technical Solutions:

  - Mobile-first CSS with proper responsive breakpoints
  - Conflicting rule resolution between universal and
  hero-specific styles
  - Smart text wrapping vs strategic text shortening

  This will ensure your mobile buttons look professional and     
  enterprise-grade across all devices! 🚀